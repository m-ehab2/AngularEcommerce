import { Routes } from '@angular/router';
import { PageHeroComponent } from './components/shared/page-hero/page-hero.component';

export const routes: Routes = [{path:'shop',component:PageHeroComponent}];
